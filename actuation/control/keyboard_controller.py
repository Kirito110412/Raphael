import time
import random

def type_text(text: str):
    """
    Stub for pyautogui typing with randomized inter-key delays (50-180ms per char).
    """
    print(f"Typing '{text}' with human-like delays.")
